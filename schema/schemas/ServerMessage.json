{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "additionalProperties": false,
    "required": [],
    "description": "An incoming websocket message from the server telling the client of a change in room state or instructing it to take an action",
    "properties": {},
    "anyOf": [
        {
            "required": ["action", "message"],
            "additionalProperties": false,
            "properties": {
                "action": "chat",
                "message": {"type": "string"}
            }
        },
        {
            "required": ["action", "row", "col", "cell"],
            "additionalProperties": false,
            "properties": {
                "action": "cellUpdate",
                "row": {"type": "number"},
                "col": {"type": "number"},
                "cell": {"$ref": "./Cell.json"}
            }
        },
        {
            "required": ["action", "board"],
            "additionalProperties": false,
            "properties": {
                "action": "syncBoard",
                "board": {"$ref": "./Board.json"}
            }
        },
        {
            "required": ["action", "board", "chatHistory"],
            "additionalProperties": false,
            "properties": {
                "action": "connected",
                "board": {"$ref": "./Board.json"},
                "chatHistory": {"type": "array", "items": {"type": "string"} },
                "nickname": {"type": "string"}
            }
        },
        {
            "required": ["action"],
            "additionalProperties": false,
            "properties": {
                "action": "unauthorized"
            }
        }
    ]
}